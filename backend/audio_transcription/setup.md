## Installation Requirements

This module requires `pkg-config` and `ffmpeg` to be installed on your system.

### macOS
brew install pkg-config ffmpeg

### Ubuntu / Linux
sudo apt update
sudo apt install pkg-config ffmpeg

### Windows
Download FFmpeg:
https://www.gyan.dev/ffmpeg/builds/

Then add FFmpeg to PATH.
(No pkg-config needed on Windows.)
